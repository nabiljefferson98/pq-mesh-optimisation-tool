% =============================================================================
% chapter4_results.tex — Chapter 4: Results
% =============================================================================
\chapter{Results}
\label{chap:results}

% TODO: Write full prose for each section.
% The scaffolding below marks every sub-section with its data source,
% key argument, and the figure / table already committed to the repository.

% ---------------------------------------------------------------------------
\section{Scalability Benchmark (EXP-01)}
\label{sec:results_scalability}
% KEY ARGUMENT:
%   T(n) ≈ 0.0007 × n^{1.27}, R² = 1.000; interactive threshold (<2 s)
%   met for meshes up to 400 faces (20×20 grid).
% DATA SOURCE: docs/results/EXP-01_scalability.md

% TODO: Write §4.1 prose here.

\input{figures}  % Figure 4.2 — log-log scalability plot
                 % Figure 4.3 — convergence + scaling overlay

% ---------------------------------------------------------------------------
\section{Backend Comparison (EXP-02)}
\label{sec:results_backend}
% KEY ARGUMENT:
%   Numba achieves 2.4–2.8× speedup; CuPy achieves 2.2–3.1× speedup.
%   Maximum numerical difference between backends < 4.1×10⁻⁷ (well below gtol).
% DATA SOURCE: docs/results/EXP-02_backend_comparison.md

% TODO: Write §4.2 prose here.

% ---------------------------------------------------------------------------
\section{Convergence Analysis (EXP-03)}
\label{sec:results_convergence}
% KEY ARGUMENT:
%   9–13 L-BFGS-B iterations regardless of mesh size;
%   mean |d_f| < 3×10⁻⁴ by iteration 5 (< 0.3 mm per metre of panel).
% DATA SOURCE: docs/results/EXP-03_convergence.md

% TODO: Write §4.3 prose here.

% ---------------------------------------------------------------------------
\section{Weight Sensitivity (EXP-04)}
\label{sec:results_weight}
% KEY ARGUMENT:
%   Default weights (w_p=10, w_f=1, w_c=5) achieve 99.99\% planarity
%   improvement. A factor-of-two change in any single weight produces
%   < 0.1\% change in final planarity.
% DATA SOURCE: docs/results/EXP-04_weight_sensitivity.md

% TODO: Write §4.4 prose here.

% ---------------------------------------------------------------------------
\section{Real-World Dataset Evaluation (EXP-05)}
\label{sec:results_realworld}
% KEY ARGUMENT:
%   4/4 real-world meshes converge. Oloid near-zero absolute deviation
%   (mean |d_f| = 0.0032) confirms developable surface theory.
% DATA SOURCE: docs/results/EXP-05_realworld.md

% TODO: Write §4.5 prose here.

% Benchmark table (EXP-05 real-world performance)
\input{../../data/output/tables/benchmark_table}

% Figure 4.8 — planarity histograms
\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{EXP-05_planarity_histograms}
  \caption{Per-face planarity deviation $|d_f|$ histograms before (grey)
    and after (blue) optimisation for all four EXP-05 real-world meshes
    (Spot, Blub, Oloid-256, Bob). Post-optimisation distributions are
    concentrated near zero, confirming convergence. The oloid histogram
    is truncated at $|d_f| = 0.01$ as the surface is already
    near-developable prior to optimisation.}
  \label{fig:planarity_histograms}
\end{figure}

% Figure 4.9 — oloid spatial heatmap
\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.80\textwidth]{EXP-05_oloid_spatial_heatmap}
  \caption{Spatial distribution of per-face planarity deviation $|d_f|$
    on the optimised oloid-256 mesh. Residual deviation is concentrated
    along the circular boundary curves, consistent with the theoretical
    prediction for a developable surface under planar-quad constraints.}
  \label{fig:oloid_heatmap}
\end{figure}
